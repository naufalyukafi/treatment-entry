export default class Categories {
    nameCategory: string;
    id: string;
    constructor(
        id: string,
        nameCategory: string,
    ) {
        this.id = id;
        this.nameCategory = nameCategory;
    }
}
